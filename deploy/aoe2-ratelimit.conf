# Rate limit para el proxy del asistente IA.
# Ubicación en el servidor: /etc/nginx/conf.d/aoe2-ratelimit.conf
# (debe ir en contexto http, por eso va en conf.d/ y no en el server block)
limit_req_zone $binary_remote_addr zone=aoe2anthropic:10m rate=20r/m;
